# wotrlay Configuration Example
# Copy this file to .env and adjust values as needed

# Trust score threshold above which all event kinds are allowed
# Range: 0.0 - 1.0
# Default: 0.5
# Below this threshold, only Kind 1 events are allowed
MID_THRESHOLD=0.5

# Trust score threshold above which backfill is free and max rate applies
# Range: 0.0 - 1.0
# Optional - if not set, there is no distinct high tier (3-tier system)
# Must be greater than MID_THRESHOLD if set
# Above this threshold, old events (>24h) skip rate limiting
# If undefined, all pubkeys with r ≥ midThreshold get maximum rate (10,000/day)
# HIGH_THRESHOLD=0.9

# Maximum rank refresh requests per day per IP group
# Default: 100
# Protects the rank-queue from abuse by limiting refresh attempts
RANK_QUEUE_IP_DAILY_LIMIT=100

# ContextVM relay URL for rank lookups
# Default: wss://relay.contextvm.org
RELATR_RELAY=wss://relay.contextvm.org

# Relatr service pubkey
# Default: 750682303c9f0ddad75941b49edc9d46e3ed306b9ee3335338a21a3e404c5fa3
RELATR_PUBKEY=750682303c9f0ddad75941b49edc9d46e3ed306b9ee3335338a21a3e404c5fa3

# Secret key for signing rank requests (optional)
# If not provided, a temporary key will be auto-generated for the session
# RELATR_SECRET_KEY=your-secret-key-here

# URL_POLICY_ENABLED is optional - set to "true" to restrict URLs below MID_THRESHOLD
# URL_POLICY_ENABLED="true"  # uncomment to enable URL policy

# NIP-11 Relay Information Document Configuration
# Relay name for NIP-11 info document
# Default: wotrlay
RELAY_NAME=wotrlay

# Relay description for NIP-11 info document
# Default: A Web-of-Trust (WoT) based Nostr relay with reputation-driven rate limiting
RELAY_DESCRIPTION=A Web-of-Trust (WoT) based Nostr relay with reputation-driven rate limiting

# Relay public key (optional) - leave empty if not applicable
# Default: (empty)
# RELAY_PUBKEY=your-relay-pubkey-here

# Relay contact information (optional) - email or nostr NIP-05 identifier
# Default: (empty)
# RELAY_CONTACT=admin@example.com

# Software URL for NIP-11 info document
# Default: https://github.com/user/wotrlay
SOFTWARE=https://github.com/user/wotrlay

# Software version for NIP-11 info document
# Default: 0.1.0
VERSION=0.1.0